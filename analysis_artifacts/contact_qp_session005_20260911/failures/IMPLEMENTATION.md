# Implemented follow-up

Root follow-up: the staged supervisor patch was independently reviewed and applied to `/media/camp/EXT_DRIVE/ICRA_YM/script/scan_robot.py`. Installed SHA256 is `475651e872615123a75624d9bfe37d876ec01dddbe23843c41ca3a149d597b0b`; original backup is `scan_robot.py.before_apply`. References below to “staged / not written” describe the worker handoff before root application.

This supersedes the read-only design section of README.md after the root explicitly authorized implementation and the user directed that unstable data should not itself stop scanning.

- `ContactQpOuter` snapshots the immutable feature before taking its decision timestamp, so a feature arriving asynchronously during nominal computation cannot be falsely classified as future.
- `feature.dropout_policy='pause_visual'` discards stale/missing images and images with invalid required boundary windows after one valid compatible image in contact. It removes the visual observation from the QP indefinitely while fresh-force control continues. Invalid boundary flags cannot establish startup validity or falsely report recovery. Transition logs mark degraded scan quality and recovery; rate-limited terminal messages show visual paused/resumed. Legacy default `stop` remains supported with optional finite `dropout_grace_s` measured from the first unavailable control decision. Startup absence/invalidity, version mismatch and genuine future/invalid timestamps remain rejected.
- Force-source deadline misses classified at final review or pre-send dispatch abort rail reservation, outer nominal/energy reservation, and inner/native proposal before sending. Fresh recomputation is bounded by the unchanged previous committed lease and existing watchdog/external-stop checks; retry count is diagnostic. A same-source next tick waits with normal pacing before any proposal is prepared. No send, heartbeat, command-history commit, reference advance or lease renewal occurs during this wait. An unavailable or expired previous lease, other admission failures, rail faults and partial/unknown sends still stop.
- `energy.task_power_source` constructor wiring and the conditional nominal-twist snapshot keyword integrate the separately implemented logical task allowance. Startup identifies the source and shared-total-velocity fusion.
- The external supervisor change is staged in `scan_robot_completion.patch` / `scan_robot.py.staged`, not written to ICRA_YM. It distinguishes done_seq acknowledgment from confirmed DONE or a successful icra_wait handoff, preserving the current message when termination is unsuccessful. Existing controller IPC exposes err_code/status/msg, but msg is truncated to90 characters. No supervisor timeout or force threshold is changed.

Validation: 73 tests passed across the new transient feedback suite, existing active publication/command budget/continuous visual suites, and the staged supervisor branch tests, including the ULTRA review fix for required-window validity. The tests execute production Python publication and native request-flag blocks without starting devices. Diff whitespace checks passed. Dependencies were supplied by the existing genesis Python plus rm75 cmeel site packages; no packages were installed.

Limits: this has no hardware validation. The source clock's declared interval/age checks remain strict. A new source after a true long gap can therefore still trigger the existing cadence bound; the no-send retry does not disguise that gap or replay stale commands. Watchdog/old-lease expiry still ends a persistent force outage. The true-future/version policy remains a rejection and should be separately reviewed if runtime policy is expanded. The root/ULTRA reviewer should review these control-flow branches before any live execution.
