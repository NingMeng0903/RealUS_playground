# /001 独立 ULTRA 审查

审查者：独立 `gpt-6-astra` agent，`reasoning_effort=ultra`；只读代码审查与离线测试。范围为本轮 /001 地轨功率模型、两级内层功率约束、未发送回滚和模式切换力发布。

最终结论：**无剩余阻断发现**。

- 最终地轨模型排除观测重定位，保留真实发布限位修改。
- Python/native 两级 QP 使用完整命令 Jacobian 的六轴冻结功率约束。
- 数值边界使用 W 单位；零力行保持可行；最终账本不放宽容差。
- 审查发现并已修正：native 最终拒绝时也需恢复 committed history。Python 两次已提交速度的 jerk 历史已修正。
- 模式切换补偿力使用独立 observer 和运动学 Data；消费不同的源时间戳，防止恢复期间旧发布覆盖新样本。
- 能量留额、旧 epoch 责任、名义供能和冻结配对结算保持原义。

独立验证：Python/relay/daemon **40 项通过**；允许本机 IPC 的环境中 native **6 项通过**，其中三组实际低能量输入各 400 次连续发布/结算，另含不可行、零力、失败历史回滚。差异空白检查通过。

首次 native 检查被沙箱本机 IPC 限制阻止，在允许本机 IPC 的离线测试环境中复验通过。未接触机器人、地轨或外部网络。

局限：不构成实机时序保证、物理被动性证明、声学质量改善证据，也不保证机械约束冲突时仍可完成扫描。
