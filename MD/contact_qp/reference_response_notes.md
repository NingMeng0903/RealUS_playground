A/B bounded design correction evidence

The native v9 experiment was archived and withdrawn; see experimental_native_v9/. Stage 1 loop was restored byte-for-byte to its recorded SHA256. Baseline protocol v8 rebuilt successfully. Withdrawal checks: 97 passed, 1 deselected (100k long run); native baseline 13 passed, 1 pre-existing stale source-string test deselected.

Reference: original ForearmReference L geometry and 0.4 s smooth ramp shared across variants. Feedback uses accepted p(s); feedforward uses bounded candidate displacement divided by full execution dt. Reference commit uses alpha*h_ref only after publication. Machine-precision clock exhaustion is allowed only after positive alpha publication; zero alpha freezes time. Mechanical completion distance remains 1e-4 m. Final-ramp exact endpoint-pose equality is now separately recorded as reference_geometry_roundoff completion evidence, without advancing an alpha-zero clock. ULTRA A/B implementation PASS is recorded in review_reference_response_v2.md.

reference_interval_replays.json contains all eight prior full endpoint failures and three matched-scan examples after the smooth-reference fix, all completed with zero mechanical violations. That process used the pre-B response policy. reference_shadow_a_only.json reloads the frozen design_v2 response implementation to isolate A: both shadow seed0 full/full_consistency completed after the clock-ULP fix. reference_shadow_replays.json includes the current B policy. Intermediate preview-target replay failures are retained under reference_interval_intermediate_preview_replays.*; that target choice was rejected during audit.

Response consistency remains an optional repair-only ablation. It waits for an explicit 0.08 s image-effective window and 10 um actual net positive local displacement, resets on significant intervening unloading, and discounts near-zero or wrong response. It does not discount based on command proposals, future post-exposure motion, texture changes without motion, missing images, or across registration/measurement discontinuities. It does not change alpha loss or claim to solve persistent-shadow slow scanning.

Reference/image telemetry now records h_ref, h_exec, frame identity, effective/received times, image age and quality version. Tail age advances with actual time; no extra frame is fabricated. Source hashes: reference_response_sources.sha256. Bounded tests: reference_response_tests.log (38 passed). No held-out run or large new design set was started.
