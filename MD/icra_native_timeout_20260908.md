# ICRA 扫描后 native_timeout 检查

## 本次故障

对应快照：`rm75_control/apps/logs/peirastic/faults/qpik_fault_1788875824675659242_621346.json`。
快照时间为 2026-09-08 21:57:04（Asia/Shanghai），记录阶段为 `servo_twist`；扫描完成后的保持仍运行控制循环。

用户日志中的同一请求 97601：

| 诊断 | 数值 | 含义 |
|---|---:|---|
| 第一次等待 | 20.1 ms | 超过 20 ms 单次等待，先 coast/hold |
| 最终请求年龄 | 51.5 ms | 超过既有约 50 ms 请求年龄上限，锁存停止 |
| native_cpu_ms | 2.57 ms | 从提交请求的基准到诊断采样，native 实际占用 CPU 的累计增量 |
| native_runqueue_ms | 45.46 ms | 同一区间内 native 可运行、但等待 CPU 的累计增量 |
| last_completed_solve_ms | 2.30 ms | 上一条完成请求的求解耗时，不是本条过期请求的求解耗时 |

该日志直接支持严重调度等待，不能把 51.5 ms 全部算成 QP 求解时间。
`observed_reply_seq=97601` 是诊断打印时再次读取；它不证明结果在请求过期前可用。过期结果继续拒绝，未放宽 20 ms / 50 ms 保护或加入自动 RESET。

## 当前资源检查及修正范围

- 本机控制配置为 CPU 2，native CPU 4；SMT 同胞分别为 2/3 和 4/5。
- 当前超声 UI PID 589446 使用全部 32 个逻辑 CPU，nice=0，共 36 个线程。只读 1.002 秒采样累计约 641 ms CPU；线程数不等于同时占用 36 个核。
- `us_framegrab/scripts/run_ui.py` 没有应用现有 `prepare_observer_process()`。补充启动时资源设置，在 Qt/OpenCV 和采集子进程启动前限制数值线程、降优先级并排除控制核心及其 SMT 同胞，覆盖 UI 与 headless。
- 现场另有未限制 CPU 的离线解剖模型验证任务。其启动时间早于此次故障，但本次没有调度跟踪记录，不能唯一归因于某个进程。未终止或调整其他正在运行的任务。
- 新的启动设置需要退出旧超声节点后重新启动；只修改文件不会改变已有线程及采集子进程的调度配置。

这些设置只限制相关观察进程自身，并非系统层面的 CPU 独占。其他未限制的程序、内核任务仍可能竞争控制核心；无法据此承诺绝不超时。

## 同时出现的滑轨信息

`FA24 latched without encoder` 表示最近 FA24 命令非零、编码器超过约 120 ms 未成功更新。安全路径 `emergency_zero_fa24()` 会主动关闭主 Modbus socket，经临时连接写零后重连，因此紧接着的 `not connected` 也可能来自保护动作，并不能单凭这一行判断物理网线断开。三次连续通信失败会进入 HOLD。此次快照中滑轨反馈年龄约 14.6 ms，后续超时日志又显示 connected/armed；这些记录不能重建完整断连过程，也不能证明它是 native 调度排队的直接原因。

## 验证

- 超声入口 bootstrap 和 Qt 导入顺序两个测试通过，UI/headless 在实际相机环境的 `--help` 均不启动硬件。
- 相机环境执行新 bootstrap 的只读子进程核验：有效 CPU mask 为 0/1/6–31，nice=10，数值线程为 1，尚未导入 Qt/OpenCV。
- native 通知/超时保护的 13 项测试通过。没有接受过期解或增加等待上限。

本次未发送机器人运动、恢复或急停解除命令，未改变控制器超时阈值。
