# Query-side Region A only. These extents are not network inputs or training labels.
position_box:
  tangent_m: 0.003
  binormal_m: 0.004
  normal_m: 0.002

orientation_cone:
  half_angle_deg: 3.0

sampling:
  kind: fixed_joint_sobol
  dimensions: 5
  samples: 64
  seed: 17

aggregation:
  kind: normalized_softmin
  softmin_tau: 0.15
  coverage_tau: 0.15
