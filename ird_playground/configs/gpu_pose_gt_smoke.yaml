out: data/ird/gpu_pose_smoke.npz
sampling:
  robot_spec: configs/robot_probe45.yaml
  n_fk_positive: 512
  n_pose_queries: 2048
  n_ik_seeds: 6
  batch_size: 128
  near_query_fraction: 0.5
  seed: 42
  device: cuda
  log_every_batches: 2
  default_branch_id: 0
