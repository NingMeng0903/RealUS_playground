# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual envs
.venv/
venv/
env/

# Secrets / local env
.env
.env.*
*.local.yaml

# Local Taichi libcuda.so shim (genesis/.cuda_shim/)
**/.cuda_shim/

# Force compensation: regenerated locally (collection / identification)
data/force_compensation/logs/*.npz
data/force_compensation/logs/*.log
data/force_compensation/logs/force_id_phi.json

# Scan / rail debug CSV (opt-in via --log-csv)
apps/logs/

# Rail software-zero (counts0); regenerated by apps/lw100_rail_home_limit.py
var/lw100_rail_zero.json
var/lw100_rail_zero.json.tmp

# Legacy velocity-admittance npz captures
data/velocity_admittance/logs/*.npz

# C++ QPIK kernel (rebuild with native/qpik_kernel/build.sh)
native/qpik_kernel/build/
rm75_control/control/joint_admittance_8dof/solver/_qpik_kernel*.so

# OS / editor
.DS_Store
*.swp
*~
.idea/
.vscode/
