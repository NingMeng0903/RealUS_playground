parents:
  - config/data/multivideo.yml
args:
  loadmp: True 
  compose_mp: True
  pids: [0, 1]
  filter:
    min_conf: 0.3
    # bound: 0.05 # Enabling this drops feet at the bottom edge and causes bad optimization
  keys:
    _no_merge_: True
    keypoints2d: keypoints